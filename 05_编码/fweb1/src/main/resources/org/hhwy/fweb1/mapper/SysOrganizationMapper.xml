<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.hhwy.fweb1.mapper.SysOrganizationMapper">

  <insert id="insert" parameterType="SysOrganization">
    insert into t_sys_organization(id, organization_name, create_time, create_user, update_time, update_user, description) values ( #{id}, #{organizationName}, now(), #{createUser}, now(), #{createUser}, #{description} )
  </insert>

  <update id="update" parameterType="map">
    update t_sys_organization set <if test="organizationName != ''">organization_name = #{organizationName},</if>  update_time = now(), <if test="updateUser != ''">update_user = #{updateUser},</if> <if test="description != ''">description = #{description},</if> version = version + 1 where id = #{id}
  </update>

  <delete id="delete" parameterType="string">
    delete from t_sys_organization where id = #{id}
  </delete>

  <select id="get" parameterType="string" resultType="SysOrganization">
    select id as id, organization_name as organizationName, create_time as createTime, create_user as createUser, update_time as updateTime, update_user as updateUser, description as description, version as version from t_sys_organization where id = #{id}
  </select>
  
  <select id="getVersion" parameterType="string" resultType="int">
    select version from t_sys_organization where id = #{id}
  </select>

  <select id="getAll" resultType="SysOrganization">
    select id as id, organization_name as organizationName, create_time as createTime, create_user as createUser, update_time as updateTime, update_user as updateUser, description as description, version as version from t_sys_organization
  </select>

</mapper>

