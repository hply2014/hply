<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.hhwy.fweb1.mapper.SysAuthorizationMapper">

  <insert id="insert" parameterType="SysAuthorization">
    insert into t_sys_authorization(id, resource_id, user_id, create_time, create_user) values ( #{id}, #{resourceId}, #{userId}, #{createTime}, #{createUser} )
  </insert>

  <update id="update" parameterType="map">
    update t_sys_authorization set resource_id = #{resourceId}, user_id = #{userId}, create_time = #{createTime}, create_user = #{createUser},  version = version + 1 where id = #{id}
  </update>

  <delete id="delete" parameterType="string">
    delete from t_sys_authorization where id = #{id}
  </delete>

  <select id="get" parameterType="string" resultType="SysAuthorization">
    select id as Id, resource_id as ResourceId, user_id as UserId, create_time as CreateTime, create_user as CreateUser, version as Version from t_sys_authorization where id = #{id}
  </select>
  
  <select id="getVersion" parameterType="string" resultType="int">
    select version from t_sys_authorization where id = #{id}
  </select>

  <select id="getAll" resultType="SysAuthorization">
    select id as Id, resource_id as ResourceId, user_id as UserId, create_time as CreateTime, create_user as CreateUser, version as Version from t_sys_authorization
  </select>

</mapper>

