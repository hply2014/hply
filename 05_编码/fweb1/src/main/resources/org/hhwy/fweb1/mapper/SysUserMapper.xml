<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.hhwy.fweb1.mapper.SysUserMapper">

  <insert id="insert" parameterType="SysUser">
    insert into t_sys_user(id, login_name, real_name, organization_id, password, last_login_ip, last_login_time, fails, logined, position, enabled, order_by, create_time, create_user, update_time, update_user, description, must_change_password) values ( #{id}, #{loginName}, #{realName}, #{organizationId}, #{password}, #{lastLoginIp}, #{lastLoginTime}, #{fails}, #{logined}, #{position}, #{enabled}, #{orderBy}, #{createTime}, #{createUser}, #{updateTime}, #{updateUser}, #{description}, #{mustChangePassword} )
  </insert>

  <update id="update" parameterType="map">
    update t_sys_user set login_name = #{loginName}, real_name = #{realName}, organization_id = #{organizationId}, password = #{password}, last_login_ip = #{lastLoginIp}, last_login_time = #{lastLoginTime}, fails = #{fails}, logined = #{logined}, position = #{position}, enabled = #{enabled}, order_by = #{orderBy}, create_time = #{createTime}, create_user = #{createUser}, update_time = #{updateTime}, update_user = #{updateUser}, description = #{description},  version = version + 1, must_change_password = #{mustChangePassword} where id = #{id}
  </update>

  <delete id="delete" parameterType="string">
    delete from t_sys_user where id = #{id}
  </delete>

  <select id="get" parameterType="string" resultType="SysUser">
    select id as Id, login_name as LoginName, real_name as RealName, organization_id as OrganizationId, password as Password, last_login_ip as LastLoginIp, last_login_time as LastLoginTime, fails as Fails, logined as Logined, position as Position, enabled as Enabled, order_by as OrderBy, create_time as CreateTime, create_user as CreateUser, update_time as UpdateTime, update_user as UpdateUser, description as Description, version as Version, must_change_password as MustChangePassword from t_sys_user where id = #{id}
  </select>
  
  <select id="getVersion" parameterType="string" resultType="int">
    select version from t_sys_user where id = #{id}
  </select>

  <select id="getAll" resultType="SysUser">
    select id as Id, login_name as LoginName, real_name as RealName, organization_id as OrganizationId, password as Password, last_login_ip as LastLoginIp, last_login_time as LastLoginTime, fails as Fails, logined as Logined, position as Position, enabled as Enabled, order_by as OrderBy, create_time as CreateTime, create_user as CreateUser, update_time as UpdateTime, update_user as UpdateUser, description as Description, version as Version, must_change_password as MustChangePassword from t_sys_user
  </select>

</mapper>

