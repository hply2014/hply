<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.hhwy.rdc.fweb1.mapper.CollectionsMapper">

  <insert id="insert" parameterType="Collections">
    insert into t_collections(id, project_id, ticket_code, source_of, payment_type, amount, bank_account, bank_name, trice, create_time, create_user, update_time, update_user, description) values ( #{id}, #{projectId}, #{ticketCode}, #{sourceOf}, #{paymentType}, #{amount}, #{bankAccount}, #{bankName}, #{trice}, #{createTime}, #{createUser}, #{updateTime}, #{updateUser}, #{description} )
  </insert>

  <update id="update" parameterType="map">
    update t_collections set project_id = #{projectId}, ticket_code = #{ticketCode}, source_of = #{sourceOf}, payment_type = #{paymentType}, amount = #{amount}, bank_account = #{bankAccount}, bank_name = #{bankName}, trice = #{trice}, create_time = #{createTime}, create_user = #{createUser}, update_time = #{updateTime}, update_user = #{updateUser}, description = #{description},  version = version + 1 where id = #{id}
  </update>

  <delete id="delete" parameterType="string">
    delete from t_collections where id = #{id}
  </delete>

  <select id="get" parameterType="string" resultType="Collections">
    select id as Id, project_id as ProjectId, ticket_code as TicketCode, source_of as SourceOf, payment_type as PaymentType, amount as Amount, bank_account as BankAccount, bank_name as BankName, trice as Trice, create_time as CreateTime, create_user as CreateUser, update_time as UpdateTime, update_user as UpdateUser, description as Description, version as Version from t_collections where id = #{id}
  </select>
  
  <select id="getVersion" parameterType="string" resultType="int">
    select version from t_collections where id = #{id}
  </select>

  <select id="getAll" resultType="Collections">
    select id as Id, project_id as ProjectId, ticket_code as TicketCode, source_of as SourceOf, payment_type as PaymentType, amount as Amount, bank_account as BankAccount, bank_name as BankName, trice as Trice, create_time as CreateTime, create_user as CreateUser, update_time as UpdateTime, update_user as UpdateUser, description as Description, version as Version from t_collections
  </select>

</mapper>

