<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hply.persistence.SysAuthorizationMapper">

	<cache />

	<insert id="insert" useGeneratedKeys="true" keyProperty="id"
		parameterType="SysAuthorization">
		insert into t_sys_authorization(id, resource_id,
		user_id, create_time,
		create_user) values ( #{id}, #{resource_id},
		#{user_id},
		#{create_time}, #{create_user} )
	</insert>

	<update id="update" parameterType="map">
		update t_sys_authorization set
		resource_id = #{resource_id}, user_id =
		#{user_id}, create_time =
		#{create_time}, create_user = #{create_user}
		where id = #{id}
	</update>

	<delete id="delete">
		delete from t_sys_authorization where id = #{id}
	</delete>

	<select id="get" parameterType="string" resultType="SysAuthorization">
		select * from
		t_sys_authorization where id = #{id}
	</select>

	<select id="getAll" resultType="SysAuthorization">
		select * from t_sys_authorization
	</select>

</mapper>


