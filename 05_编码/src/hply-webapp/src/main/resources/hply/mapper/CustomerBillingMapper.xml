<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hply.mapper.CustomerBillingMapper">

  <cache />
  
  
  <select id="getAllByProject" resultType="CustomerBilling">
    select id as id, project_id as projectId, invoice_code as invoiceCode, invoice_type as invoiceType, amount as amount, trice as trice, create_time as createTime, create_user as createUser, update_time as updateTime, update_user as updateUser, description as description, version as version, status as status from t_customer_billing where project_id=#{projectId} order by trice desc
  </select>
  
  <select id="getRowCountByOrganization" resultType="int">
    select count(*) from t_customer_billing t where exists (select 0 from t_project p where t.project_id=p.id and p.organization_id=#{organizationId})
  </select>

  <select id="getAllPagedByOrganization" resultType="CustomerBilling">
    select id as id, project_id as projectId, invoice_code as invoiceCode, invoice_type as invoiceType, amount as amount, trice as trice, create_time as createTime, create_user as createUser, update_time as updateTime, update_user as updateUser, description as description, version as version, status as status from t_customer_billing t where exists (select 0 from t_project p where t.project_id=p.id and p.organization_id=#{organizationId}) order by trice desc limit #{pageSize} offset #{pageIndex}
  </select>
</mapper>

