<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hply.mapper.ContractChangeMapper">

  <insert id="insert" parameterType="ContractChange">
    insert into t_contract_change(id, project_id, csa_code, management_rate, change_amount, trice, create_time, create_user, update_time, update_user, description, status, field_01, field_02, field_03, field_04, field_05) values ( #{id}, #{projectId}, #{csaCode}, #{managementRate}, #{changeAmount}, #{trice}, now(), #{createUser}, now(), #{createUser}, #{description}, #{status}, #{field01}, #{field02}, #{field03}, #{field04}, #{field05} )
  </insert>

  <update id="update" parameterType="map">
    update t_contract_change set <if test="projectId != ''">project_id = #{projectId},</if> <if test="csaCode != ''">csa_code = #{csaCode},</if> <if test="managementRate != null">management_rate = #{managementRate},</if> <if test="changeAmount != null">change_amount = #{changeAmount},</if> <if test="trice != null">trice = #{trice},</if>  update_time = now(), <if test="updateUser != ''">update_user = #{updateUser},</if> <if test="description != ''">description = #{description},</if> <if test="status != null">status = #{status},</if> <if test="field01 != ''">field_01 = #{field01},</if> <if test="field02 != ''">field_02 = #{field02},</if> <if test="field03 != ''">field_03 = #{field03},</if> <if test="field04 != ''">field_04 = #{field04},</if> <if test="field05 != ''">field_05 = #{field05},</if> version = version + 1 where id = #{id}
  </update>

  <delete id="delete" parameterType="string">
    delete from t_contract_change where id = #{id}
  </delete>

  <select id="get" parameterType="string" resultType="ContractChange">
    select id as id, project_id as projectId, csa_code as csaCode, management_rate as managementRate, change_amount as changeAmount, trice as trice, create_time as createTime, create_user as createUser, update_time as updateTime, update_user as updateUser, description as description, version as version, status as status, field_01 as field01, field_02 as field02, field_03 as field03, field_04 as field04, field_05 as field05 from t_contract_change where id = #{id}
  </select>
  
  <select id="getVersion" parameterType="string" resultType="int">
    select version from t_contract_change where id = #{id}
  </select>

  <select id="getAll" resultType="ContractChange">
    select id as id, project_id as projectId, csa_code as csaCode, management_rate as managementRate, change_amount as changeAmount, trice as trice, create_time as createTime, create_user as createUser, update_time as updateTime, update_user as updateUser, description as description, version as version, status as status, field_01 as field01, field_02 as field02, field_03 as field03, field_04 as field04, field_05 as field05 from t_contract_change
  </select>

</mapper>

