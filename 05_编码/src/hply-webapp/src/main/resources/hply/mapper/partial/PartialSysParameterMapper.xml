<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hply.mapper.partial.PartialSysParameterMapper">

  <insert id="insert" parameterType="SysParameter">
    insert into t_sys_parameter(id, en_name, cn_name, param_value, default_value, is_inner, create_time, create_user, update_time, update_user, description, field_01, field_02, field_03, field_04, field_05) values ( #{id}, #{enName}, #{cnName}, #{paramValue}, #{defaultValue}, #{isInner}, now(), #{createUser}, now(), #{createUser}, #{description}, #{field01}, #{field02}, #{field03}, #{field04}, #{field05} )
  </insert>

  <update id="update" parameterType="map">
    update t_sys_parameter set en_name = #{enName}, cn_name = #{cnName}, param_value = #{paramValue}, default_value = #{defaultValue}, is_inner = #{isInner},  update_time = now(), update_user = #{updateUser}, description = #{description}, field_01 = #{field01}, field_02 = #{field02}, field_03 = #{field03}, field_04 = #{field04}, field_05 = #{field05}, version = version + 1 where id = #{id}
  </update>

  <delete id="delete" parameterType="string">
    delete from t_sys_parameter where id = #{id}
  </delete>

  <select id="get" parameterType="string" resultType="SysParameter">
    select id as id, en_name as enName, cn_name as cnName, param_value as paramValue, default_value as defaultValue, is_inner as isInner, create_time as createTime, create_user as createUser, update_time as updateTime, update_user as updateUser, description as description, version as version, field_01 as field01, field_02 as field02, field_03 as field03, field_04 as field04, field_05 as field05 from t_sys_parameter where id = #{id}
  </select>
  
  <select id="getVersion" parameterType="string" resultType="int">
    select version from t_sys_parameter where id = #{id}
  </select>
  
  <select id="getAll" resultType="SysParameter">
    select id as id, en_name as enName, cn_name as cnName, param_value as paramValue, default_value as defaultValue, is_inner as isInner, create_time as createTime, create_user as createUser, update_time as updateTime, update_user as updateUser, description as description, version as version, field_01 as field01, field_02 as field02, field_03 as field03, field_04 as field04, field_05 as field05 from t_sys_parameter
  </select>
  
  <select id="getRowCount" resultType="int">
    select count(*) from t_sys_parameter
  </select>

  <select id="getAllPaged" parameterType="SysParameter" resultType="SysParameter">
    select id as id, en_name as enName, cn_name as cnName, param_value as paramValue, default_value as defaultValue, is_inner as isInner, create_time as createTime, create_user as createUser, update_time as updateTime, update_user as updateUser, description as description, version as version, field_01 as field01, field_02 as field02, field_03 as field03, field_04 as field04, field_05 as field05 from t_sys_parameter limit #{pageSize} offset #{pageIndex}
  </select>
</mapper>

