<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hply.mapper.partial.PartialProjectTaxRateMapper">

  <insert id="insert" parameterType="ProjectTaxRate">
    insert into t_project_tax_rate(id, project_id, name, tax_rate, amount, create_time, create_user, update_time, update_user, description, status, field_01, field_02, field_03, field_04, field_05) values ( #{id}, #{projectId}, #{name}, #{taxRate}, #{amount}, now(), #{createUser}, now(), #{createUser}, #{description}, #{status}, #{field01}, #{field02}, #{field03}, #{field04}, #{field05} )
  </insert>

  <update id="update" parameterType="map">
    update t_project_tax_rate set project_id = #{projectId}, name = #{name}, tax_rate = #{taxRate}, amount = #{amount},  update_time = now(), update_user = #{updateUser}, description = #{description}, status = #{status}, field_01 = #{field01}, field_02 = #{field02}, field_03 = #{field03}, field_04 = #{field04}, field_05 = #{field05}, version = version + 1 where id = #{id}
  </update>

  <delete id="delete" parameterType="string">
    delete from t_project_tax_rate where id = #{id}
  </delete>
  
  <select id="get" parameterType="string" resultType="ProjectTaxRate">
    select id as id, project_id as projectId, name as name, tax_rate as taxRate, amount as amount, create_time as createTime, create_user as createUser, update_time as updateTime, update_user as updateUser, description as description, version as version, status as status, field_01 as field01, field_02 as field02, field_03 as field03, field_04 as field04, field_05 as field05 from t_project_tax_rate where id = #{id}
  </select>
  
  <select id="getVersion" parameterType="string" resultType="int">
    select version from t_project_tax_rate where id = #{id}
  </select>
  
  <select id="getAll" resultType="ProjectTaxRate">
    select id as id, project_id as projectId, name as name, tax_rate as taxRate, amount as amount, create_time as createTime, create_user as createUser, update_time as updateTime, update_user as updateUser, description as description, version as version, status as status, field_01 as field01, field_02 as field02, field_03 as field03, field_04 as field04, field_05 as field05 from t_project_tax_rate
  </select>
  
  <select id="getRowCount" resultType="int">
    select count(*) from t_project_tax_rate
  </select>

  <select id="getAllPaged" parameterType="ProjectTaxRate" resultType="ProjectTaxRate">
    select id as id, project_id as projectId, name as name, tax_rate as taxRate, amount as amount, create_time as createTime, create_user as createUser, update_time as updateTime, update_user as updateUser, description as description, version as version, status as status, field_01 as field01, field_02 as field02, field_03 as field03, field_04 as field04, field_05 as field05 from t_project_tax_rate limit #{pageSize} offset #{pageIndex}
  </select>
</mapper>

