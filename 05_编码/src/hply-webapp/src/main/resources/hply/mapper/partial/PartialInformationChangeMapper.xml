<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hply.mapper.partial.PartialInformationChangeMapper">

  <insert id="insert" parameterType="InformationChange">
    insert into t_information_change(id, information_id, original_data, new_data, create_time, create_user, update_time, update_user, description, status, field_01, field_02, field_03, field_04, field_05) values ( #{id}, #{informationId}, #{originalData}, #{newData}, now(), #{createUser}, now(), #{createUser}, #{description}, #{status}, #{field01}, #{field02}, #{field03}, #{field04}, #{field05} )
  </insert>

  <update id="update" parameterType="map">
    update t_information_change set information_id = #{informationId}, original_data = #{originalData}, new_data = #{newData},  update_time = now(), update_user = #{updateUser}, description = #{description}, status = #{status}, field_01 = #{field01}, field_02 = #{field02}, field_03 = #{field03}, field_04 = #{field04}, field_05 = #{field05}, version = version + 1 where id = #{id}
  </update>

  <delete id="delete" parameterType="string">
    delete from t_information_change where id = #{id}
  </delete>

  <select id="get" parameterType="string" resultType="InformationChange">
    select id as id, information_id as informationId, original_data as originalData, new_data as newData, create_time as createTime, create_user as createUser, update_time as updateTime, update_user as updateUser, description as description, version as version, status as status, field_01 as field01, field_02 as field02, field_03 as field03, field_04 as field04, field_05 as field05 from t_information_change where id = #{id}
  </select>
  
  <select id="getVersion" parameterType="string" resultType="int">
    select version from t_information_change where id = #{id}
  </select>
  
  <select id="getAll" resultType="InformationChange">
    select id as id, information_id as informationId, original_data as originalData, new_data as newData, create_time as createTime, create_user as createUser, update_time as updateTime, update_user as updateUser, description as description, version as version, status as status, field_01 as field01, field_02 as field02, field_03 as field03, field_04 as field04, field_05 as field05 from t_information_change
  </select>
  
  <select id="getRowCount" resultType="int">
    select count(*) from t_information_change
  </select>

  <select id="getAllPaged" parameterType="InformationChange" resultType="InformationChange">
    select id as id, information_id as informationId, original_data as originalData, new_data as newData, create_time as createTime, create_user as createUser, update_time as updateTime, update_user as updateUser, description as description, version as version, status as status, field_01 as field01, field_02 as field02, field_03 as field03, field_04 as field04, field_05 as field05 from t_information_change limit #{pageSize} offset #{pageIndex}
  </select>
</mapper>

