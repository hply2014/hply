<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hply.persistence.SysAuthorizationMapper">

  <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="hply.domain.SysAuthorization">
    insert into t_sys_authorization(ID, ResourceID, UserID, CreateTime, CreateUser) values ( #{ID}, #{ResourceID}, #{UserID}, #{CreateTime}, #{CreateUser} )
  </insert>

  <update id="update" parameterType="map">
    update t_sys_authorization set ResourceID = #{ResourceID}, UserID = #{UserID}, CreateTime = #{CreateTime}, CreateUser = #{CreateUser} where id = #{id}
  </update>

  <delete id="delete" parameterType="string">
    delete from t_sys_authorization where id = #{id}
  </delete>

  <select id="get" parameterType="string" resultType="hply.domain.SysAuthorization">
    select * from t_sys_authorization where id = #{id}
  </select>

  <select id="getAll" resultType="hply.domain.SysAuthorization">
    select * from t_sys_authorization
  </select>

</mapper>


