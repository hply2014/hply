<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hply.mapper.ProjectTaxRateMapper">
	<!-- <cache /> -->

	<delete id="deleteBy" parameterType="map">
	  delete from t_project_tax_rate where (${where})
	</delete>
  
    <select id="getAllBy" parameterType="map" resultType="ProjectTaxRate">
      select id as id, project_id as projectId, name as name, tax_rate as taxRate, amount as amount, create_time as createTime, create_user as createUser, update_time as updateTime, update_user as updateUser, description as description, version as version, status as status, field_01 as field01, field_02 as field02, field_03 as field03, field_04 as field04, field_05 as field05 from t_project_tax_rate where (${where}) order by create_time 
    </select>
    
    
    <select id="getGroupByTaxTate" parameterType="map" resultType="map">
      select tax_rate as taxRate,sum(amount) as amount from t_project_tax_rate where (${where}) group by tax_rate
    </select>
</mapper>

