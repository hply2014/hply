<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hply.mapper.partial.PartialPaymentDifferentTaxAmountMapper">
  <cache/>

  <insert id="insert" parameterType="PaymentDifferentTaxAmount">
    insert into t_payment_different_tax_amount(id, payment_id, name, amount, create_time, create_user, update_time, update_user, description, status, field_01, field_02, field_03, field_04, field_05) values ( #{id}, #{paymentId}, #{name}, #{amount}, now(), #{createUser}, now(), #{createUser}, #{description}, #{status}, #{field01}, #{field02}, #{field03}, #{field04}, #{field05} )
  </insert>

  <update id="update" parameterType="map">
    update t_payment_different_tax_amount set payment_id = #{paymentId}, name = #{name},  amount = #{amount},  update_time = now(), update_user = #{updateUser}, description = #{description}, status = #{status}, field_01 = #{field01}, field_02 = #{field02}, field_03 = #{field03}, field_04 = #{field04}, field_05 = #{field05}, version = version + 1 where id = #{id}
  </update>

  <delete id="delete" parameterType="string">
    delete from t_payment_different_tax_amount where id = #{id}
  </delete>
  
  <select id="get" parameterType="string" resultType="PaymentDifferentTaxAmount">
    select id as id, payment_id as paymentId, name as name,  amount as amount, create_time as createTime, create_user as createUser, update_time as updateTime, update_user as updateUser, description as description, version as version, status as status, field_01 as field01, field_02 as field02, field_03 as field03, field_04 as field04, field_05 as field05 from t_payment_different_tax_amount where id = #{id}
  </select>
  
  <select id="getVersion" parameterType="string" resultType="int">
    select version from t_payment_different_tax_amount where id = #{id}
  </select>
  
  <select id="getAll" resultType="PaymentDifferentTaxAmount">
    select id as id, payment_id as paymentId, name as name,  amount as amount, create_time as createTime, create_user as createUser, update_time as updateTime, update_user as updateUser, description as description, version as version, status as status, field_01 as field01, field_02 as field02, field_03 as field03, field_04 as field04, field_05 as field05 from t_payment_different_tax_amount
  </select>
  
  <select id="getRowCount" resultType="int">
    select count(*) from t_payment_different_tax_amount
  </select>

  <select id="getAllPaged" parameterType="PaymentDifferentTaxAmount" resultType="PaymentDifferentTaxAmount">
    select id as id, payment_id as paymentId, name as name,  amount as amount, create_time as createTime, create_user as createUser, update_time as updateTime, update_user as updateUser, description as description, version as version, status as status, field_01 as field01, field_02 as field02, field_03 as field03, field_04 as field04, field_05 as field05 from t_payment_different_tax_amount limit #{pageSize} offset #{pageIndex}
  </select>
</mapper>

